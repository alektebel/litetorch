# Core dependencies
numpy>=1.20.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional benchmark dependencies are available via:
# pip install -e .[benchmark]
# This installs: torch, stable-baselines3, gymnasium
